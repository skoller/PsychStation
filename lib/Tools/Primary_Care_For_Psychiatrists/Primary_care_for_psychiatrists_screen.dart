import 'package:flutter/material.dart';

class PrimaryCareForPsychiatristsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Primary Care for Psychiatrists"),
      ),
      body: Container(
       child: Text('these are the topics for primary care for psychiatrists'),
      ),
    );
  }
}