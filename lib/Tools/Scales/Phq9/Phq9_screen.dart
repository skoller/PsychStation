import 'package:flutter/material.dart';

class Phq9Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("PHQ9"),
      ),
      body: Container(
       child: Text('this is the PHQ9'),
      ),
    );
  }
}