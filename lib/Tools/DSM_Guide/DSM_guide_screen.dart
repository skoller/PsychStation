import 'package:flutter/material.dart';

class DSMGuideScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("DSM Guide"),
      ),
      body: Container(
       child: Text('these are the DSM Guides'),
      ),
    );
  }
}