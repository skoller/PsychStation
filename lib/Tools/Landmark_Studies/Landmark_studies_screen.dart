import 'package:flutter/material.dart';

class LandmarkStudiesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Landmark Studies"),
      ),
      body: Container(
       child: Text('these are the landmark studies'),
      ),
    );
  }
}
